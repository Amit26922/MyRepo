class Car: #creating a class 
    make = "Mercedes benz" #declaring and defining class attributes and their variables
    colour = "black"
    ModelClass = "S"

car = Car() #creating an object from that created class

print(f"details of the car make->{car.make}, colour ->{car.colour}, model->{car.ModelClass}") #accessing the classing variables directly through the object using dot(.) opeartor
